function Footer(){
    return(
        <footer>
            <p>&copy; {new Date().getFullYear()} Keidy's Personal Portfolio</p>
        </footer>
    );
};

export default Footer;
//this is how you incorporate html, js, and react component arch in one component